<h1>Edit Students</h1>
<%= form_for(@student) do |s| %>
    <%= s.label :first_name %>
    <%= s.text_area :first_name %>
    <%= s.label :last_name %>
    <%= s.text_area :last_name %>
    <%= s.submit %>
<% end %>